engines:
  tailor:
    enabled: true
    checks:
      trailing-whitespace:
        enabled: false
      terminating-newline:
        enabled: false
  fixme:
    enabled: true
  spellcheck:
    enabled: true

ratings:
  paths:
  - "**.swift"
